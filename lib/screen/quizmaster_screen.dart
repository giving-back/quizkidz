// Flutter imports:
import 'package:flutter/material.dart';

class QuizmasterScreen extends StatelessWidget {
  const QuizmasterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Quizmaster');
  }
}
